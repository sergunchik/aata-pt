<?xml version="1.0"?>
<!-- This file is part of the book                 -->
<!--                                               -->
<!--   Abstract Algebra: Theory and Applications   -->
<!--                                               -->
<!-- Copyright (C) 2010-2014  Robert A. Beezer     -->
<!-- See the file COPYING for copying conditions.  -->
<!--                                               -->
<!-- This file is automatically generated from the -->
<!-- private solution manual.  Minor contributed   -->
<!-- edits can be made against this file for       -->
<!-- the purpose of a pull request.                -->
<exercises xml:id="vect-sage-exercises">
    <title>Ejercicios en Sage</title>

    <exercise number="1">
        <statement>

            <p>Dados dos subespacios <m>U</m> y <m>W</m> de un espacio vectorial <m>V</m>, su suma <m>U+W</m> puede ser definida como el <em>conjunto</em> <m>U+W=\{u+w\mid u\in U,\ w\in W\}</m>, en otras palabras, el conjunto de todas las sumas posibles de un elemento de <m>U</m> y un elemento de <m>W</m>.</p>

            <p>Note que esto no es la suma directa del texto, ni corresponde al método <c>direct_sum()</c> en Sage.  Pero, es posible construir este subespacio en Sage como sigue.  Tome bases de <m>U</m> y <m>W</m> por separado, para definir listas de vectores.  Junte las dos listas usando el signo de suma entre ellas (esto concatena las listas).  Ahora construya el subespacio suma creando el subespacio de <m>V</m> generado por este conjunto, usando el método <c>.subspace()</c>.</p>

            <p>En el espacio vectorial (<c>QQ^10</c>) construya dos subespacios que cumplan que (a) tengan dimensión <m>5</m> o <m>6</m>, y (b) tengan una intersección de dimensión <m>2</m>.  Compare sus dimensiones individuales con las dimensiones de su intersección (<m>U\cap W</m>, <c>.intersection()</c> en Sage) y su suma <m>U+W</m>.</p>

            <p>Repita el experimento con espacios vectoriales de dimensión <m>8</m>, y con intersección tan pequeña como sea posible.  Conjeture una relación entre estas cuatro dimensiones basado en los resultados de sus experimentos.</p>

        </statement>

        
    </exercise>

    <exercise number="2">
        <statement>

            <p>Podemos construir un cuerpo en Sage que extienda los racionales agregando una raíz cuarta de dos, <m>{\mathbb Q}[\sqrt[4]{2}]</m>, con el comando <c>F.&lt;c&gt; = QQ[2^(1/4)]</c>.  Este es un espacio vectorial de dimensión <m>4</m> sobre los racionales, con una base que consiste de las primeras cuatro potencias de <m>c = \sqrt[4]{2}</m> (partiendo de la potencia cero).</p>

            <p>El comando <c>F.vector_space()</c> le devolverá estos tres ítemes en una tripleta (de manera que tenga cuidado como usa esta salida para extraer lo que necesite).  La primera componente de  la salida es un espacio vectorial sobre los racionales que es isomorfo a <c>F</c>.  La siguiente es un isomorfismo de espacios vectoriales (una transformación linea invertible) del espacio entregado al cuerpo, mientras la tercera componente es un isomorfismo en la dirección opuesta.  Estos dos isomorfismo pueden ser usados como funciones.  Note que este es un comportamiento distinto al obtenido con el método <c>.vector_space()</c> aplicado a cuerpos finitos.  Construya ejemplos no triviales que muestren que estos isomorfismos de espacios vectoriales se comportan como deben los isomorfismos.</p>

        </statement>

        
    </exercise>

    <exercise number="3">
        <statement>

            <p>Construya un cuerpo finito <m>F</m> de orden <m>p^n</m> en la forma usual.  Luego construya el grupo (multiplicativo) de todas las matrices invertibles (no singulares) de <m>m\times m</m> sobre este cuerpo con el comando <c>G = GL(m, F)</c> (<q>el grupo lineal general</q>).  ¿Cuál es el orden de este grupo?  En otras palabras, encuentre una expresión general para el orden de este grupo.</p>

            <p>Su respuesta debiese ser en función de <m>m</m>, <m>p</m> y <m>n</m>.  Explique su solución en detalle y verifique con ejemplos en Sage que su respuesta es correcta.</p>

            <p>Ayudas:  <c>G.order()</c> le ayudará a poner a prueba y verificar sus hipótesis.  Ejemplos pequeños en Sage (listando todos los elementos del grupo) pueden ayudar a su intuición<mdash/>que es la razón de que esto sea un ejercicio en Sage.  Pequeños quiere decir matrices de <m>2\times 2</m> y <m>3\times 3</m> y cuerpos finitos con <m>2,3,4,5</m> elementos, a lo sumo.  Los resultados no dependen realmente de <m>p</m> y <m>n</m>, sino solo de <m>p^n</m>.</p>

            <p>Advierta que este grupo es interesante porque contiene representaciones de todas las transformaciones lineales invertibles del espapcio vectorial <m>F^m</m> en sí mismo.</p>

        </statement>

        
    </exercise>

    <exercise number="4">
        <statement>

            <p>¿Qué pasa si intentamos hacer álgebra lineal sobre un <em>anillo</em> que no sea un <em>cuerpo</em>?  El objeto que más se parece a un espacio vectorial, pero con esta diferencia, se conoce como <term>módulo</term> (no confundir con las congruencias módulo algo).  Usted puede obtener uno fácilmente con una construcción como <c>ZZ^3</c>.  Ejecute la siguiente celda para crear un módulo y un submódulo.</p>

            <sage>
            <input>
            M = ZZ^3
            u = M([1, 0, 0])
            v = M([2, 2, 0])
            w = M([0, 0, 4])
            N = M.submodule([u, v, w])
            </input>
            </sage>

            <p>Examine las bases y las dimensiones (es decir <q>rango</q>) del módulo y del submódulo, y verifique si el módulo y el submódulo son iguales.  ¿Cómo se diferencia esto de la situación análoga para espacios vectoriales?  ¿Puede crear un tercer módulo, <c>P</c>, que sea un subconjunto propio de <c>M</c> y que contenga propiamente a <c>N</c>?</p>

        </statement>

        
    </exercise>

    <exercise number="5">
        <statement>

            <p>Un cuerpo finito, <m>F</m>, de orden <m>5^3</m> es un espacio vectorial de dimensión 3 sobre <m>{\mathbb Z}_5</m>.  Supongamos que <m>a</m> es un generador de <m>F</m>.  Sea <m>M</m> cualquier matriz de <m>3\times 3</m> con coeficientes en <m>{\mathbb Z}_5</m> (cuidado acá, los elementos son del cuerpo de escalares, no del espacio vectorial).  Si convertimos un elemento <m>x\in F</m> en un vector (relativo a la base <m>\{1,a,a^2\}</m>), entonces podemos multiplicarlo por <m>M</m> (con <m>M</m> al lado izquierdo) para crear otro vector, que entonces podemos traducir en una combinación lineal de los elementos de la base, y por ende en otro elemento de <m>F</m>.  Esta función es un homomorfismo de espacios vectoriales, mejor conocido como una transformación lineal (implementeda con su representación matricial relativa a la base <m>\{1,a,a^2\}</m>.  Note que cada parte más abajo se vuelve menos general y más específica.</p>

            <ol>
                <li><p>Cree una matriz no-invertible <m>R</m> y dé ejemplos para mostrar que la función descrita por <m>R</m> es un homomorfismo de espacios vectoriales de <m>F</m> en <m>F</m>.</p></li>

                <li><p>Cree una matriz invertible <m>M</m>.  La función ahora será un homomorfismo invertible.  Determine la función inversa y dé ejemplos para verificar sus propiedades.</p></li>

                <li><p>Como <m>a</m> es un generador del cuerpo, la función <m>a\mapsto a^5</m> puede ser extendida a un homomorfismo de espacios vectoriales (<ie/> una transformación lineal). Encuentre una matriz <m>M</m> que efectúe esta transformación lineal, y de ahí determine que el homomorfismo es invertible.</p></li>

                <li><p>Ninguna de las tres partes anteriores utiliza las propiedades de la multiplicación en el cuerpo.  Pero la función de la tercera parte también preserva la multiplicación en el cuerpo, aunque esto puede no ser obvio en este momento.  Estamos afirmando que esta última función es un automorfismo de cuerpos, preservando tanto la suma como la multiplicación.  Dé un ejemplo no-trivial de la propiedad de preservación del producto de esta función.  (Esta es la <term>función de Frobenius</term> que será discutida en mayor detalle en el Capítulo<nbsp/><xref ref="fields"/>.)</p></li>
            </ol>

        </statement>

        
    </exercise>

</exercises>
